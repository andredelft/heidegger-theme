from setuptools import setup, find_packages


setup(
    name="heidegger_theme",
    version="1.0",
    description="Heidegger translations",
    packages=find_packages(),
    include_package_data=True,
)
